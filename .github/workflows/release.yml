name: Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  FLUTTER_VERSION: '3.24.3'

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            artifact_name: local-llm-linux.tar.gz
            build_path: build/linux/x64/release/bundle
          - os: macos-latest
            platform: macos
            artifact_name: local-llm-macos.zip
            build_path: build/macos/Build/Products/Release
          - os: windows-latest
            platform: windows
            artifact_name: local-llm-windows.zip
            build_path: build/windows/x64/runner/Release
            
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        cache: true
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Install Linux dependencies
      if: matrix.platform == 'linux'
      run: |
        sudo apt-get update -y
        sudo apt-get install -y ninja-build libgtk-3-dev
        
    - name: Enable desktop support
      run: |
        flutter config --enable-${{ matrix.platform }}-desktop
        
    - name: Build application
      run: flutter build ${{ matrix.platform }} --release
      
    - name: Create archive (Linux)
      if: matrix.platform == 'linux'
      run: |
        cd ${{ matrix.build_path }}
        tar -czf ../../../../../${{ matrix.artifact_name }} .
        
    - name: Create archive (macOS)
      if: matrix.platform == 'macos' 
      run: |
        cd ${{ matrix.build_path }}
        zip -r ../../../../../${{ matrix.artifact_name }} .
        
    - name: Create archive (Windows)
      if: matrix.platform == 'windows'
      run: |
        cd ${{ matrix.build_path }}
        powershell Compress-Archive -Path * -DestinationPath ../../../../../${{ matrix.artifact_name }}
        
    - name: Get version from tag
      id: get_version
      shell: bash
      run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        
    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        name: Release ${{ steps.get_version.outputs.VERSION }}
        body: |
          ## 🚀 Nova Versão: ${{ steps.get_version.outputs.VERSION }}
          
          ### 📋 Principais Mudanças
          - Melhorias na interface do usuário
          - Otimizações de performance
          - Correções de bugs
          - Novas funcionalidades de busca estratégica
          - Circuit Breaker Pattern implementado
          - Rate Limiting avançado
          
          ### 📦 Downloads
          Escolha o arquivo apropriado para seu sistema operacional:
          - **Linux**: `local-llm-linux.tar.gz`
          - **macOS**: `local-llm-macos.zip`
          - **Windows**: `local-llm-windows.zip`
          
          ### 🔧 Requisitos do Sistema
          - Sistema operacional: Linux, macOS ou Windows
          - Ollama instalado e configurado
          - Conexão com internet para funcionalidades de busca
          
          ### 🔄 Novidades desta versão
          - **Busca Web Inteligente**: Sistema multi-estratégia com fallback
          - **Circuit Breaker**: Proteção contra falhas em cascata
          - **Rate Limiting**: Controle inteligente de requisições
          - **Monitoramento**: Health checks automáticos das estratégias
          
          ---
          
          🤖 Generated with [Claude Code](https://claude.ai/code)
        files: ${{ matrix.artifact_name }}
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}